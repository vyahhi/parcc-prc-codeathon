default:
  autoload:
    '': %paths.base%/features/bootstrap
  suites:
    default:
      paths:
        - %paths.base%/features
  extensions:
    Behat\MinkExtension:
      base_url:  'http://prc-poc.local:8083'
      sessions:
        default:
          selenium2: ~