default:
  paths:
    features: 'features'
  extensions:
    Behat\MinkExtension\Extension:
      selenium2: ~
      goutte: ~
      base_url: http://prc-poc.local:8083/
    Drupal\DrupalExtension\Extension:
      api_driver: "drupal"
      drupal:
        drupal_root: "/Users/jfranks/Sites/devdesktop/prc-poc/docroot"
      blackbox: ~
      # Set the drush alias to "@self" by default, when executing tests from within the drupal installation.
      drush:
        root: /Users/jfranks/Sites/devdesktop/prc-poc/docroot
