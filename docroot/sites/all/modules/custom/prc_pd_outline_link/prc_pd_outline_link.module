<?php

/**
 * Implements hook_ctools_plugin_directory().
 */
function prc_pd_outline_link_ctools_plugin_directory($owner, $plugin_type) {
  if ($owner == 'ctools') {
    return "plugins/$plugin_type";
  }
}